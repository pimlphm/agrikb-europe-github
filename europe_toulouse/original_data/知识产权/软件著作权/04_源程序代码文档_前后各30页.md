# 基于工业本体的民机适航知识图谱智能分析系统

# 源程序代码文档

> **说明**：根据中国版权保护中心要求，提供源程序的前30页和后30页（约60页），
> 每页不少于50行。以下按模块顺序排列。实际提交时需打印为A4纸质版。

---

## ========== 前30页（程序开头部分） ==========

### 文件1: main.py （应用入口）

```python
"""
民机适航知识库Agent
========================================
面向空客/波音民机设计团队的适航知识图谱系统
集成经验积累与审计引擎
"""
import sys
import os
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent
os.chdir(str(BASE_DIR))
sys.path.insert(0, str(BASE_DIR))

import yaml
from PyQt5.QtWidgets import QApplication, QSplashScreen, QMessageBox
from PyQt5.QtGui import QPixmap, QFont, QPainter, QColor
from PyQt5.QtCore import Qt


def load_config():
    config_path = BASE_DIR / "config.yaml"
    if config_path.exists():
        with open(config_path, "r", encoding="utf-8") as f:
            return yaml.safe_load(f)
    return {
        "backend": "ollama",
        "ollama": {"base_url": "http://localhost:11434",
                   "model": "qwen2.5:7b-instruct",
                   "temperature": 0.3, "max_tokens": 4096},
        "vllm_ascend": {"base_url": "http://localhost:8000",
                        "model": "Qwen/Qwen2.5-7B-Instruct",
                        "temperature": 0.3, "max_tokens": 4096,
                        "api_key": "EMPTY"},
        "ontology": {"graph_db": "ontology/knowledge_graph.json"},
    }


def create_splash(backend_display):
    pix = QPixmap(560, 360)
    pix.fill(QColor("#0d1117"))
    p = QPainter(pix)
    p.setPen(QColor("#58a6ff"))
    p.setFont(QFont("Microsoft YaHei", 24, QFont.Bold))
    p.drawText(pix.rect().adjusted(0, 30, 0, -180), Qt.AlignCenter,
               "知识库 Agent")
    p.setPen(QColor("#8b949e"))
    p.setFont(QFont("Microsoft YaHei", 11))
    p.drawText(pix.rect().adjusted(0, 85, 0, -140), Qt.AlignCenter,
               "IOF/BFO Ontology Knowledge Graph System")
    p.setPen(QColor("#3fb950"))
    p.setFont(QFont("Microsoft YaHei", 10))
    p.drawText(pix.rect().adjusted(0, 130, 0, -100), Qt.AlignCenter,
               f"推理后端 Backend: {backend_display}")
    colors = ["#FF0000", "#FF6600", "#FFCC00", "#33CC33",
              "#00CCCC", "#3366FF", "#9933FF"]
    labels = ["赤", "橙", "黄", "绿", "青", "蓝", "紫"]
    start_x = (560 - 7 * 36) // 2
    p.setFont(QFont("Microsoft YaHei", 9))
    for i, (c, l) in enumerate(zip(colors, labels)):
        x = start_x + i * 36
        p.fillRect(x, 190, 30, 16, QColor(c))
        p.setPen(QColor("#ffffff"))
        p.drawText(x, 190, 30, 16, Qt.AlignCenter, l)
    p.end()
    return QSplashScreen(pix)


def main():
    app = QApplication(sys.argv)
    app.setFont(QFont("Microsoft YaHei", 10))
    config = load_config()
    backend = config.get("backend", "ollama")
    backend_display = "昇腾910B vLLM" if backend == "vllm_ascend" else "Ollama Local"
    splash = create_splash(backend_display)
    splash.show()
    app.processEvents()
    splash.showMessage("初始化推理后端...",
                       Qt.AlignBottom | Qt.AlignCenter, QColor("#58a6ff"))
    app.processEvents()
    from core.llm_agent import create_llm_client, AgentEngine
    llm = create_llm_client(config)
    if not llm.check_connection():
        splash.close()
        QMessageBox.warning(None, "连接警告", "推理后端未连接，系统将以离线模式启动。")
    splash.showMessage("初始化知识图谱引擎...",
                       Qt.AlignBottom | Qt.AlignCenter, QColor("#58a6ff"))
    app.processEvents()
    from core.ontology_engine import OntologyEngine
    ontology = OntologyEngine(
        db_path=config.get("ontology", {}).get("graph_db",
                                                "ontology/knowledge_graph.json"))
    splash.showMessage("初始化经验引擎...",
                       Qt.AlignBottom | Qt.AlignCenter, QColor("#58a6ff"))
    app.processEvents()
    from core.experience_engine import ExperienceEngine
    experience = ExperienceEngine(str(BASE_DIR), llm_client=llm)
    splash.showMessage("初始化适航标准库...",
                       Qt.AlignBottom | Qt.AlignCenter, QColor("#58a6ff"))
    app.processEvents()
    from core.standards_library import StandardsLibrary
    standards = StandardsLibrary(str(BASE_DIR))
    agent = AgentEngine(llm, ontology, experience_engine=experience,
                        standards_library=standards)
    splash.showMessage("启动界面...",
                       Qt.AlignBottom | Qt.AlignCenter, QColor("#58a6ff"))
    app.processEvents()
    from gui.main_window import MainWindow
    window = MainWindow(agent, ontology, config, experience, standards)
    window.show()
    splash.finish(window)
    sys.exit(app.exec_())


if __name__ == "__main__":
    main()
```

### 文件2: core/ontology_engine.py （本体引擎开头）

```python
"""
通用Ontology知识图谱引擎
=========================
架构设计融合:
  - Hedi Karray (UTTOP/Toulouse INP) 的 IOF 分层本体
  - IOF/BFO (Basic Formal Ontology) 三层架构
  - 关联强度量化: 赤(1.0)→橙→黄→绿→青→蓝→紫(0.1) 七级色谱
"""
import json
import os
import uuid
import math
from datetime import datetime
from typing import Optional
from pathlib import Path
from collections import defaultdict

from rdflib import Graph as RDFGraph, Namespace, URIRef, Literal
from rdflib.namespace import RDF, RDFS, OWL, XSD
import networkx as nx

BFO = Namespace("http://purl.obolibrary.org/obo/BFO_")
IOF = Namespace("https://spec.industrialontologies.org/ontology/core/Core/")
AERO = Namespace("http://aviation-kb.org/ontology#")
KB = Namespace("http://aviation-kb.org/node#")

STRENGTH_SPECTRUM = [
    {"name": "赤", "min": 0.86, "max": 1.00, "color": "#FF0000", "label": "极强"},
    {"name": "橙", "min": 0.72, "max": 0.85, "color": "#FF6600", "label": "强"},
    {"name": "黄", "min": 0.58, "max": 0.71, "color": "#FFCC00", "label": "中强"},
    {"name": "绿", "min": 0.44, "max": 0.57, "color": "#33CC33", "label": "中等"},
    {"name": "青", "min": 0.30, "max": 0.43, "color": "#00CCCC", "label": "弱"},
    {"name": "蓝", "min": 0.16, "max": 0.29, "color": "#3366FF", "label": "间接"},
    {"name": "紫", "min": 0.00, "max": 0.15, "color": "#9933FF", "label": "潜在"},
]

TYPE_HIERARCHY = {
    "entity":       {"layer": "bfo", "parent": None,
                     "label": "实体", "icon": "◉", "base_color": "#c9d1d9"},
    "process":      {"layer": "bfo", "parent": "entity",
                     "label": "过程", "icon": "▶", "base_color": "#3fb950"},
    "function":     {"layer": "bfo", "parent": "entity",
                     "label": "功能", "icon": "⚡", "base_color": "#f0883e"},
    "quality":      {"layer": "bfo", "parent": "entity",
                     "label": "质量/属性", "icon": "◆", "base_color": "#d2a8ff"},
    "role":         {"layer": "bfo", "parent": "entity",
                     "label": "角色", "icon": "👤", "base_color": "#79c0ff"},
    "disposition":  {"layer": "bfo", "parent": "entity",
                     "label": "倾向", "icon": "↗", "base_color": "#56d364"},
    # ... (IOF核心层 + 航空领域层完整定义，共74个类型)
}
```

> **注**：以上仅展示前30页代码的结构性摘录。实际提交时，应连续打印源代码文件
> main.py全文、ontology_engine.py前半部分，每页50行以上。

---

## ========== 后30页（程序结尾部分） ==========

### 文件: gui/main_window.py （GUI结尾部分，含标准库Tab和关于页面）

```python
    # ================================================================
    #  标准库 Tab
    # ================================================================

    def _create_standards_tab(self):
        tab = QWidget()
        layout = QVBoxLayout(tab)
        layout.setContentsMargins(8, 8, 8, 8)

        toolbar = QHBoxLayout()
        self.std_search_input = QLineEdit()
        self.std_search_input.setPlaceholderText(
            "搜索标准 (如: DO-178C, 安全评估, EWIS ...)")
        self.std_search_input.returnPressed.connect(self._do_search_standards)
        toolbar.addWidget(self.std_search_input, 3)

        btn_search = QPushButton("搜索")
        btn_search.setStyleSheet("background:#1f6feb; font-weight:bold;")
        btn_search.clicked.connect(self._do_search_standards)
        toolbar.addWidget(btn_search)

        self.std_authority_combo = QComboBox()
        self.std_authority_combo.addItems(["全部当局", "FAA", "EASA", "CAAC"])
        self.std_authority_combo.currentIndexChanged.connect(
            self._filter_standards_by_authority)
        toolbar.addWidget(self.std_authority_combo)

        self.std_category_combo = QComboBox()
        self.std_category_combo.addItem("全部类别", "")
        from core.standards_library import CATEGORY_LABELS
        for cat_key, (cat_zh, _) in CATEGORY_LABELS.items():
            self.std_category_combo.addItem(cat_zh, cat_key)
        self.std_category_combo.currentIndexChanged.connect(
            self._filter_standards_by_category)
        toolbar.addWidget(self.std_category_combo)
        layout.addLayout(toolbar)

        splitter = QSplitter(Qt.Vertical)
        self.std_tree = QTreeWidget()
        self.std_tree.setHeaderLabels(["标准编号", "名称", "类别", "当局"])
        self.std_tree.setAlternatingRowColors(True)
        self.std_tree.currentItemChanged.connect(self._on_standard_selected)
        splitter.addWidget(self.std_tree)
        self.std_detail = QTextBrowser()
        self.std_detail.setOpenExternalLinks(True)
        splitter.addWidget(self.std_detail)
        layout.addWidget(splitter)
        self._populate_standards_tree()
        return tab

    def _populate_standards_tree(self, authority_filter="", category_filter=""):
        self.std_tree.clear()
        if not self.standards:
            return
        authority_groups = {}
        for authority, auth_data in self.standards.db.items():
            if authority_filter and authority != authority_filter:
                continue
            for std_key, std in auth_data.get("standards", {}).items():
                cat = std.get("category", "")
                if category_filter and cat != category_filter:
                    continue
                if authority not in authority_groups:
                    authority_groups[authority] = []
                authority_groups[authority].append((std_key, std))
        from core.standards_library import CATEGORY_LABELS
        for authority in ["FAA", "EASA", "CAAC"]:
            if authority not in authority_groups:
                continue
            items = authority_groups[authority]
            auth_zh = self.standards.db[authority]["authority_zh"]
            parent = QTreeWidgetItem(
                self.std_tree,
                [f"■ {auth_zh} ({len(items)}项)", "", "", authority])
            parent.setForeground(0, QColor("#58a6ff"))
            parent.setExpanded(True)
            for std_key, std in items:
                std_id = std.get("id", std_key)
                title_zh = std.get("title_zh", "")
                cat_zh, _ = CATEGORY_LABELS.get(
                    std.get("category", ""), ("", ""))
                child = QTreeWidgetItem(
                    parent, [std_id, title_zh, cat_zh, authority])
                child.setData(0, Qt.UserRole, std_key)

    def _on_standard_selected(self, current, previous):
        if not current or not self.standards:
            return
        std_key = current.data(0, Qt.UserRole)
        if not std_key:
            return
        std = self.standards.get_standard(std_key)
        if not std:
            return
        from core.standards_library import CATEGORY_LABELS
        cat_zh, cat_en = CATEGORY_LABELS.get(
            std.get("category", ""), ("", ""))
        html = f'<h2 style="color:#58a6ff;">{std.get("id", "")}</h2>'
        html += f'<h3>{std.get("title_zh", "")}</h3>'
        if std.get("scope_zh"):
            html += f'<div style="background:#161b22; padding:10px;">'
            html += f'<b style="color:#3fb950;">适用范围</b><br>'
            html += f'{std["scope_zh"]}</div>'
        sections = std.get("key_sections", {})
        if sections:
            html += '<h3 style="color:#f0883e;">关键条款</h3>'
            for sec_key, sec in sections.items():
                html += f'<div style="border-left:3px solid #f0883e; '
                html += f'padding:8px 12px; margin:6px 0;">'
                html += f'<b>{sec.get("title_zh", sec_key)}</b>'
                if sec.get("content_zh"):
                    html += f'<br>{sec["content_zh"]}'
                html += '</div>'
        self.std_detail.setHtml(html)

    def _do_search_standards(self):
        keyword = self.std_search_input.text().strip()
        if not keyword or not self.standards:
            self._populate_standards_tree()
            return
        results = self.standards.search(keyword)
        self.std_tree.clear()
        from core.standards_library import CATEGORY_LABELS
        for r in results:
            cat_zh, _ = CATEGORY_LABELS.get(r.get("category", ""), ("", ""))
            item = QTreeWidgetItem(
                self.std_tree,
                [r["id"], r["title_zh"], cat_zh, r["authority"]])
            item.setData(0, Qt.UserRole, r["standard_key"])

    def _show_about(self):
        QMessageBox.about(
            self, "关于",
            '<div style="color:#c9d1d9;">'
            "<h3>民机适航知识库Agent v4.0</h3>"
            "<p>面向空客/波音民机设计团队的适航知识图谱系统</p>"
            "<hr><p><b>核心能力:</b></p><ul>"
            "<li>ARP4761安全评估</li>"
            "<li>DO-178C追溯完整性检查</li>"
            "<li>ARP4754A V模型全生命周期追溯</li>"
            "<li>变更影响分析</li>"
            "<li>IOF/BFO三层本体架构</li>"
            "<li>赤橙黄绿青蓝紫关联强度色谱</li>"
            "<li>Ollama/vLLM-Ascend双后端</li>"
            "<li>适航标准库: CAAC/FAA/EASA三方标准体系</li>"
            "</ul></div>"
        )
```

### 文件: core/standards_library.py （标准库引擎结尾部分）

```python
class StandardsLibrary:
    """适航标准库引擎"""

    def __init__(self, base_dir: str):
        self.base_dir = Path(base_dir) / "standards"
        self.base_dir.mkdir(parents=True, exist_ok=True)
        self.db = STANDARDS_DATABASE
        self._generate_all_files()

    def search(self, keyword: str) -> list[dict]:
        results = []
        kw = keyword.lower()
        for authority, auth_data in self.db.items():
            for std_key, std in auth_data.get("standards", {}).items():
                score = 0
                if kw in std.get("id", "").lower():
                    score += 10
                if kw in std.get("title_zh", "").lower():
                    score += 5
                if kw in std.get("scope_zh", "").lower():
                    score += 3
                if score > 0:
                    results.append({
                        "authority": authority,
                        "standard_key": std_key,
                        "id": std.get("id", std_key),
                        "title_zh": std.get("title_zh", ""),
                        "category": std.get("category", ""),
                        "score": score,
                    })
        results.sort(key=lambda x: -x["score"])
        return results

    def get_standard(self, standard_key: str) -> Optional[dict]:
        for authority, auth_data in self.db.items():
            standards = auth_data.get("standards", {})
            if standard_key in standards:
                std = standards[standard_key].copy()
                std["authority"] = authority
                std["authority_zh"] = auth_data["authority_zh"]
                return std
        return None

    def get_context_for_prompt(self, keywords: list[str] = None,
                                max_standards: int = 8) -> str:
        if not keywords:
            return ""
        all_results = []
        seen = set()
        for kw in keywords:
            for r in self.search(kw):
                if r["standard_key"] not in seen:
                    seen.add(r["standard_key"])
                    all_results.append(r)
        all_results.sort(key=lambda x: -x["score"])
        if not all_results:
            return ""
        lines = ["[标准库参考 - 以下为相关适航标准条款]"]
        for r in all_results[:max_standards]:
            std = self.get_standard(r["standard_key"])
            if not std:
                continue
            lines.append(f"### {std.get('id', '')} — {std.get('title_zh', '')}")
            scope = std.get("scope_zh", "")
            if scope:
                lines.append(f"  范围: {scope[:200]}")
        return "\n".join(lines)

    def _generate_all_files(self):
        db_path = self.base_dir / "standards_db.json"
        with open(db_path, "w", encoding="utf-8") as f:
            json.dump(self.db, f, ensure_ascii=False, indent=2)
        for authority, auth_data in self.db.items():
            auth_dir = self.base_dir / authority
            auth_dir.mkdir(parents=True, exist_ok=True)
            for std_key, std in auth_data.get("standards", {}).items():
                self._write_standard_md(
                    auth_dir, std_key, std, authority, auth_data)
        self._write_cross_reference_table()
```

> **注**：以上仅展示后30页代码的结构性摘录。实际提交时，应连续打印
> standards_library.py后半部分、main_window.py后半部分，每页50行以上。

---

## 提交说明

根据《计算机软件著作权登记办法》第十条规定：

1. 源程序应提交前、后各连续30页，共60页
2. 每页不少于50行
3. 如源程序不足60页，应全部提交
4. 页眉标注软件全称和版本号

**本系统总源代码行数约6000+行，远超60页要求。**
